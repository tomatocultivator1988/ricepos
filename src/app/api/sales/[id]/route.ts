import { NextResponse } from "next/server"

export async function GET() {
  return NextResponse.json({ message: "Sale API — coming soon" })
}

export async function PUT() {
  return NextResponse.json({ message: "Sale API — coming soon" })
}
